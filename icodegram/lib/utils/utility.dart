import 'package:flutter/material.dart';

const iCodePrimaryLinearGradient = LinearGradient(
    begin: Alignment.centerRight,
    colors: [Color(0xffe86B02), Color(0xfffa9541)]);

void previousScreen({context}) {
  Navigator.pop(context);
}
